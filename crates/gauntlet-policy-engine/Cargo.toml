[package]
name = "gauntlet-policy-engine"
version = "0.6.0"
edition = "2021"
description = "Deterministic zero-authority WebAssembly policy verifier component for Agent Gauntlet"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
